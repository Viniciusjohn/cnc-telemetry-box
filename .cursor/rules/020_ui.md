# UI/PWA
/dashboard?view=operator (mobile) — 4 cards: RPM, Feed, Status, Tempo.
/dashboard?view=wall (PC/TV) — grid N×M + badge Parada ≥15s.
Polling 2s (WS depois). PWA instalável (manifest + SW). Lighthouse ≥90.
