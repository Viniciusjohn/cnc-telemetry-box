# Adapters
Rota A (preferencial): MTConnect — Spindle/Speed→rpm; Path/Feedrate/Actual→feed; Controller/Execution→estado.
Rota B (fallback): SDK Mitsubishi (edge) — rpm, feed(ou override×programado), flags spindle_on/axes_moving.
