# CNC Telemetry Box v1 - exemplo de configuracao de ambiente

# --- Banco de dados Postgres (imagem oficial postgres) ---
POSTGRES_USER=cncbox
POSTGRES_PASSWORD=troque-esta-senha
POSTGRES_DB=cnc_telemetry

# URL de banco para o backend (SQLAlchemy + psycopg2)
# Em Docker Compose, o host do banco sera o servico `db` na mesma rede.
DATABASE_URL=postgresql+psycopg2://cncbox:troque-esta-senha@db:5432/cnc_telemetry

# --- Configuracoes basicas do backend ---
# Host/porta onde o FastAPI vai escutar dentro do container.
API_HOST=0.0.0.0
API_PORT=8001

# Opcional: variaveis legacy ainda suportadas (nao obrigatorias)
# TELEMETRY_DATABASE_URL=
# TELEMETRY_API_HOST=
# TELEMETRY_API_PORT=

# --- Configuracoes de sync (stub) ---
# Endpoint hipotetico da Telemetry Central (ajustar em producao).
TELEMETRY_CENTRAL_URL=https://central.cnc-telemetry.example.com/v1/ingest
# Intervalo entre tentativas de sync, em segundos.
SYNC_INTERVAL_SECONDS=60
